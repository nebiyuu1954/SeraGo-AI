# SeraGo-AI — Matching Engine

# Django core
Django>=5.0,<7
python-dotenv>=1.0
psycopg[binary]>=3.1
whitenoise>=6.6

# Matching engine — sentence embeddings for job-talent scoring
sentence-transformers>=2.2
scikit-learn>=1.3
numpy>=1.24

# API documentation
djangorestframework>=3.14
drf-spectacular>=0.27

# LLM token counting (accurate local estimate for OpenAI-compatible models)
tiktoken>=0.7

# Resume parsing — PDF text extraction (MIT licensed; do NOT swap for
# PyMuPDF/fitz, which is AGPL)
pdfplumber>=0.11

